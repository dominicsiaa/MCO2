/**
 * Represents a Watering Can tool in the game.
 * Contains the tool name, cost,
 * and exp gained from using the tool
 */
public class WateringCan extends Tool{
    /**
     * This constructor initializes a watering can object
     */
    public WateringCan() {
        super("Watering Can", 0, 0.5);
    }
}
